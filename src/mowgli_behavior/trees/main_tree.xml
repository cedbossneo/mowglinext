<?xml version="1.0" encoding="UTF-8"?>
<!--
  main_tree.xml — Mowgli BehaviorTree.CPP v4 main tree

  Production-ready mowing behavior with dynamic replanning:
    - Emergency handling (highest priority, always reactive)
    - Boundary violation guard (stop + back up if outside area)
    - Rain detection → dock and wait until dry + delay
    - GPS quality adaptation (precise/degraded nav mode)
    - Dynamic coverage replanning when obstacle map changes
    - Low battery → dock, charge, resume mowing
    - Blade control: ON only during mowing, OFF during transit/dock
    - SLAM map + obstacle persistence: save before every docking

  State constants (HighLevelStatus.msg):
    HIGH_LEVEL_STATE_NULL       = 0
    HIGH_LEVEL_STATE_IDLE       = 1
    HIGH_LEVEL_STATE_AUTONOMOUS = 2
    HIGH_LEVEL_STATE_RECORDING  = 3

  HighLevelControl commands (HighLevelControl.srv):
    COMMAND_START = 1
    COMMAND_HOME  = 2
    COMMAND_S1    = 3   (recording)
    COMMAND_S2    = 4
-->
<root BTCPP_format="4" main_tree_to_execute="MowgliMain">

  <BehaviorTree ID="MowgliMain">
    <ReactiveSequence name="Root">

      <!-- ================================================================
           GUARD 1: Emergency (always checked, highest priority)
           ================================================================ -->
      <Fallback name="EmergencyGuard">
        <Inverter><IsEmergency/></Inverter>
        <Sequence name="EmergencyHandler">
          <SetMowerEnabled enabled="false"/>
          <StopMoving/>
          <PublishHighLevelStatus state="0" state_name="EMERGENCY"/>
          <WaitForDuration duration_sec="1.0"/>
        </Sequence>
      </Fallback>

      <!-- ================================================================
           GUARD 2: Boundary violation (stop immediately if outside area)
           ================================================================ -->
      <Fallback name="BoundaryGuard">
        <Inverter><IsBoundaryViolation/></Inverter>
        <Sequence name="BoundaryHandler">
          <SetMowerEnabled enabled="false"/>
          <StopMoving/>
          <PublishHighLevelStatus state="0" state_name="BOUNDARY_VIOLATION"/>
          <BackUp backup_dist="1.0" backup_speed="0.15"/>
          <WaitForDuration duration_sec="2.0"/>
        </Sequence>
      </Fallback>

      <!-- ================================================================
           GUARD 3: Rain detection (reactive — interrupts mowing)
           Dock, wait until rain stops, then wait 30 min delay.
           ================================================================ -->
      <Fallback name="RainGuard">
        <Inverter><IsRainDetected/></Inverter>
        <Sequence name="RainDockSequence">
          <SetMowerEnabled enabled="false"/>
          <StopMoving/>
          <PublishHighLevelStatus state="1" state_name="RAIN_DETECTED_DOCKING"/>
          <SaveObstacles/>
          <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
          <NavigateToPose goal="{dock_pose}"/>
          <PublishHighLevelStatus state="1" state_name="RAIN_WAITING"/>
          <!-- Poll rain sensor every 30s until it clears -->
          <RetryUntilSuccessful num_attempts="10000">
            <Fallback>
              <Inverter><IsRainDetected/></Inverter>
              <Sequence>
                <WaitForDuration duration_sec="30.0"/>
                <AlwaysFailure/>
              </Sequence>
            </Fallback>
          </RetryUntilSuccessful>
          <!-- Rain stopped — wait 30 min before resuming -->
          <PublishHighLevelStatus state="1" state_name="RAIN_DELAY"/>
          <WaitForDuration duration_sec="1800.0"/>
        </Sequence>
      </Fallback>

      <!-- ================================================================
           GUARD 4: GPS quality adaptation (reactive)
           Adjusts Nav2 speed and inflation based on GPS fix quality.
           ================================================================ -->
      <Fallback name="GPSModeSelector">
        <Sequence name="PreciseMode">
          <IsGPSFixed/>
          <SetNavMode mode="precise"/>
        </Sequence>
        <!-- Default to degraded if GPS is not RTK fixed -->
        <SetNavMode mode="degraded"/>
      </Fallback>

      <!-- ================================================================
           MAIN LOGIC
           Priority: critical battery → mow → home → record → idle
           ================================================================ -->
      <Fallback name="MainLogic">

        <!-- ============================================================
             a. Critical battery — emergency dock, clear command
             ============================================================ -->
        <Sequence name="CriticalBatteryDock">
          <IsBatteryLow threshold="10.0"/>
          <SetMowerEnabled enabled="false"/>
          <StopMoving/>
          <PublishHighLevelStatus state="1" state_name="CRITICAL_BATTERY_DOCKING"/>
          <SaveObstacles/>
          <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
          <NavigateToPose goal="{dock_pose}"/>
          <PublishHighLevelStatus state="1" state_name="IDLE_DOCKED"/>
          <ClearCommand/>
        </Sequence>

        <!-- ============================================================
             b. Mowing (COMMAND_START = 1)
             Full cycle: undock → plan → transit → mow → dock
             Battery check is reactive: docks, charges, resumes.
             Replan check is reactive: replans when obstacles change.
             ============================================================ -->
        <Sequence name="MowingSequence">
          <IsCommand command="1"/>

          <!-- Undock: reverse away from charger (non-fatal — skip if not docked) -->
          <PublishHighLevelStatus state="2" state_name="UNDOCKING"/>
          <Fallback name="UndockOrSkip">
            <BackUp backup_dist="1.5" backup_speed="0.15"/>
            <AlwaysSuccess/>
          </Fallback>

          <!-- Compute coverage path via opennav_coverage server -->
          <PublishHighLevelStatus state="2" state_name="PLANNING"/>
          <ComputeCoverage area_index="0" first_swath_start="{first_wp}"/>

          <!-- Swath-by-swath mowing with battery awareness
               (transit to swath 1 start is handled by ExecuteSwathBySwath) -->
          <PublishHighLevelStatus state="2" state_name="MOWING"/>

          <Fallback name="CoverageWithRecovery">

            <ReactiveSequence name="AdaptiveCoverage">

              <!-- Reactive battery check: interrupts ExecuteSwathBySwath -->
              <Fallback name="BatteryGuard">
                <Inverter><NeedsDocking threshold="20.0"/></Inverter>
                <Sequence name="BatteryDockAndResume">
                  <SetMowerEnabled enabled="false"/>
                  <StopMoving/>
                  <PublishHighLevelStatus state="1" state_name="LOW_BATTERY_DOCKING"/>
                  <SaveObstacles/>
                  <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
                  <NavigateToPose goal="{dock_pose}"/>
                  <PublishHighLevelStatus state="1" state_name="CHARGING"/>
                  <!-- Wait until battery is full -->
                  <RetryUntilSuccessful num_attempts="10000">
                    <Fallback>
                      <IsBatteryAbove threshold="95.0"/>
                      <Sequence>
                        <WaitForDuration duration_sec="30.0"/>
                        <AlwaysFailure/>
                      </Sequence>
                    </Fallback>
                  </RetryUntilSuccessful>
                  <!-- Charged — undock and resume from where we left off.
                       ExecuteSwathBySwath reads ctx->next_swath_index to
                       resume from the last incomplete swath. -->
                  <PublishHighLevelStatus state="2" state_name="RESUMING_UNDOCKING"/>
                  <BackUp backup_dist="1.5" backup_speed="0.15"/>
                  <PublishHighLevelStatus state="2" state_name="MOWING"/>
                </Sequence>
              </Fallback>

              <!-- Swath-by-swath execution with retry on failure.
                   Each swath uses NavigateToPose (obstacle-aware) for transit
                   and FollowPath (RPP) for straight-line mowing. Blocked
                   swaths are automatically skipped. -->
              <RetryUntilSuccessful num_attempts="3" name="RetrySwathExecution">
                <Fallback name="ExecuteOrRecover">
                  <ExecuteSwathBySwath/>
                  <Sequence name="RecoverAndRetry">
                    <PublishHighLevelStatus state="2" state_name="RECOVERING"/>
                    <SetMowerEnabled enabled="false"/>
                    <StopMoving/>
                    <BackUp backup_dist="0.5" backup_speed="0.1"/>
                    <ClearCostmap/>
                    <WaitForDuration duration_sec="3.0"/>
                    <AlwaysFailure/>
                  </Sequence>
                </Fallback>
              </RetryUntilSuccessful>

            </ReactiveSequence>

            <!-- All retries exhausted — dock gracefully -->
            <Sequence name="FailedCoverageDock">
              <SetMowerEnabled enabled="false"/>
              <SaveObstacles/>
              <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
              <PublishHighLevelStatus state="1" state_name="COVERAGE_FAILED_DOCKING"/>
              <NavigateToPose goal="{dock_pose}"/>
              <PublishHighLevelStatus state="1" state_name="IDLE_DOCKED"/>
              <ClearCommand/>
            </Sequence>
          </Fallback>

          <!-- Mowing complete — blade off, save state, dock -->
          <SetMowerEnabled enabled="false"/>
          <PublishHighLevelStatus state="1" state_name="MOWING_COMPLETE"/>
          <SaveObstacles/>
          <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
          <NavigateToPose goal="{dock_pose}"/>
          <PublishHighLevelStatus state="1" state_name="IDLE_DOCKED"/>
          <ClearCommand/>
        </Sequence>

        <!-- ============================================================
             c. Home (COMMAND_HOME = 2)
             ============================================================ -->
        <Sequence name="HomeSequence">
          <IsCommand command="2"/>
          <SetMowerEnabled enabled="false"/>
          <StopMoving/>
          <PublishHighLevelStatus state="1" state_name="RETURNING_HOME"/>
          <SaveObstacles/>
          <SaveSlamMap map_path="/ros2_ws/maps/garden_map"/>
          <NavigateToPose goal="{dock_pose}"/>
          <PublishHighLevelStatus state="1" state_name="IDLE_DOCKED"/>
          <ClearCommand/>
        </Sequence>

        <!-- ============================================================
             d. Recording (COMMAND_S1 = 3)
             ============================================================ -->
        <Sequence name="RecordingSequence">
          <IsCommand command="3"/>
          <PublishHighLevelStatus state="3" state_name="RECORDING"/>
          <WaitForDuration duration_sec="0.5"/>
        </Sequence>

        <!-- ============================================================
             e. Idle default
             ============================================================ -->
        <Sequence name="IdleSequence">
          <PublishHighLevelStatus state="1" state_name="IDLE"/>
          <WaitForDuration duration_sec="0.5"/>
        </Sequence>

      </Fallback>
    </ReactiveSequence>
  </BehaviorTree>

</root>
